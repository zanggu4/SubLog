# === Required ===

# GitHub OAuth App credentials
# Create at: https://github.com/settings/developers → OAuth Apps → New
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Auth.js secret (generate with: pnpm dlx auth secret)
AUTH_SECRET=

# === Optional ===

# GitHub repo name for storing subscription data (default: subscription-log)
# GITHUB_REPO_NAME=subscription-log

# === Docker / Production only ===

# Base URL of the app (required behind reverse proxy)
# AUTH_URL=https://your-domain.com

# Trust the host header from reverse proxy
# AUTH_TRUST_HOST=true
